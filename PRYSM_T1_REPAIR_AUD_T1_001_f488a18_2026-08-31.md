# PRYSM T1 Repair Evidence — AUD-T1-001

Application branch: `repair/prysm-production-closure`  
Application SHA: `f488a18ccbbfff86a33a72236b18b4e708664698`

## Repair

`writer-input.js` no longer imports or invokes the action-priority/foundation
derivers. Current WriterInput projects `ScoreSet.decisionHierarchy` directly,
validates order and FindingSet membership, and preserves the hierarchy metadata
for the Writer/Judge consumer boundary.

## Proof

- `node --test src/narrative-v2/writer-input.test.js`: 10/10 PASS, including
  `T1-WRITER-01`, a two-finding reversed-priority parity case.
- `npm run verify:prysm-whole-app`: PASS on the exact SHA.
  - assembled acceptance: 82 PASS / 0 FAIL;
  - persisted hierarchy WriterInput parity: 10/10 PASS;
  - current Narrative v2 production path: 10/10 PASS.
- Controlled transports/executors only; acceptance reports zero live provider
  calls and the Narrative tests use controlled Writer/Judge seams.
- `git diff --check`: PASS; working tree clean; HEAD unchanged after the gate.
- Local and `origin/repair/prysm-production-closure` are both
  `f488a18ccbbfff86a33a72236b18b4e708664698`; ahead/behind `0/0`.

## Scope note

The broad Narrative-v2 glob suite still exposes known prompt/version fixture
drift assigned to T3. This repair did not waive it; the exact Whole-App Gate
continues to run the governed current production-path family.
